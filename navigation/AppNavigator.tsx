/**
 * AppNavigator.tsx
 * 
 * This file is deprecated. The app now uses expo-router with file-based routing.
 * All routing logic is handled through the app/ directory structure with:
 * - (auth) group for authentication screens
 * - (app) group for authenticated screens
 * 
 * See app/_layout.tsx for the root layout configuration.
 */

export default function AppNavigator() {
  // This component is no longer used
  return null;
}
